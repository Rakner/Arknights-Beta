using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class operatorBlockCount : MonoBehaviour
{
    public static int Myrthle = 1;
    public static int Siege = 2;
    public static int Jessica = 1;
    public static int Shining = 1;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       //Debug.Log(Myrthle);
    }
}
