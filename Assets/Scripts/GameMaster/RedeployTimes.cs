using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedeployTimes : MonoBehaviour
{
    public static int Myrthle = 70;
    public static int Siege = 70;
    public static int Jessica = 70;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
